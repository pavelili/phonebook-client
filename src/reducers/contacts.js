const initialState = {
  contacts: [],
  searchInput: '',
  nameInput: '',
  phoneInput: ''
};

export default function reducer(state = initialState, action){
  switch(action.type){
    default: return state;
  }
}
